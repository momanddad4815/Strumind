import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from '@/components/providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'StruMind - Next-Generation Structural Engineering',
  description: 'AI-powered cloud-native structural analysis, design, and BIM platform',
  keywords: ['structural engineering', 'analysis', 'design', 'BIM', 'cloud', 'AI'],
  authors: [{ name: 'StruMind Team' }],
}

export const viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}
